import React from 'react';

const Header = (props) => {

    return(
        <div className="Header">
            <h2>{props.tagg}</h2>            
            </div>
        )
        
    }

export default Header